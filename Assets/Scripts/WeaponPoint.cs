using UnityEngine;
using System.Collections;

public class WeaponPoint : MonoBehaviour {

    //Empty script to be able to search for as a component in grandchildren

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
